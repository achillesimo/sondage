import { BoderDirective } from './boder.directive';

describe('BoderDirective', () => {
  it('should create an instance', () => {
    const directive = new BoderDirective();
    expect(directive).toBeTruthy();
  });
});
